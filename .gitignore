node_modules/
.env
*.log
.cache/

# Corrélation Engine — Cache prix Yahoo Finance (régénéré automatiquement)
data/price_cache/
# État de déduplication runtime (régénéré, ne pas versionner)
data/event_store.json
# events_log.json est intentionnellement versionné (données historiques précieuses)

# Python bytecode
__pycache__/
*.py[cod]

# Bases de données locales (régénérées)
*.sqlite
*.db

# Local Netlify folder
.netlify

# SQLite local DB
backend/drevmbot.db
backend/*.db
backend/data/scoring_history.json

# Rapports prep session NY (générés à chaque passe par scripts/ny_morning_prep.py)
data/ny_reports/*
!data/ny_reports/.gitkeep
